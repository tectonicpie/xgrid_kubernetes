apiVersion: v1
kind: Pod
metadata:
  name: mongodb
  labels:
    app: log-mongodb
spec:
  volumes:
  - name: log-mongo-vol
    persistentVolumeClaim:
      claimName: log-pvc
  containers:
  - image: mongo
    name: container1
    command:
    - mongod
    - "--bind_ip"
    - "0.0.0.0"
    ports:
    - containerPort: 27017
    volumeMounts:
    - name: log-mongo-vol
      mountPath: /data/db